import home from '@/src/translations/locales/en/home.json';
import profile from '@/src/translations/locales/en/profile.json';
import signIn from '@/src/translations/locales/en/sign-in.json';

const resources = {
  home,
  profile,
  signIn,
} as const;

export default resources;
